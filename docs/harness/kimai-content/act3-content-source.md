# Act 3 Content Source: CLAUDE.md 회사 내규

Generated: 2026-05-29
Workspace: `/Users/byeonsanghun/goinfre/research_ai-kimai-content`
Purpose: Act 3 설명 슬라이드와 하네스 주입 소스의 기준 문서. Act 3는 Prompt, Context, CLAUDE.md를 구분한 뒤, CLAUDE.md 적용 범위와 적용 강도를 설명하고 CLAUDE.md 설정 실습으로 넘긴다.

## 1. Act 3 결정

Act 3 실습은 더 이상 단순 분류 실습이 아니다. 수강생은 별도 실습 화면에서 김아이의 CLAUDE.md 적용 범위를 선택하고, 반복해서 지킬 회사 내규를 짧게 작성하고, 과하거나 충돌하는 내규를 줄인다.

Practice ID:

```text
act3-claude-md-setting
```

CLAUDE.md 적용 범위:

```text
global CLAUDE.md = 연방법
user CLAUDE.md = 주법
project CLAUDE.md = 회사 사규
subfolder CLAUDE.md = 부서 규칙
```

로드 순서와 적용 강도:

```text
로드 순서 = global → user → project → subfolder
적용 강도 = subfolder → project → user → global
핵심 = 현재 작업 위치에 가까운 규칙이 더 구체적이고 강하다
```

## 2. Slide List

### 3-1. 회사 내규(CLAUDE.md)는 김아이가 매번 지킬 기준입니다.

Screen anchors:

```text
업무 지시(Prompt) = 지금 지시
책상 정리(Context) = 지금 자료
회사 내규(CLAUDE.md) = 매번 지킬 기준
```

Presenter flow:

```text
Act 3의 기준표를 먼저 세운다.
Act 0 목차에서 본 회사 내규(CLAUDE.md)를 다시 회수한다.
Prompt는 팀장이 지금 내린 업무 지시이고, Context는 책상 위 작업 자료이며, CLAUDE.md는 김아이가 항상 따라야 하는 회사 내규라고 자리를 나눈다.
```

Bridge:

```text
회사 내규는 김아이의 작업 방식을 매번 같은 기준으로 맞춥니다.
```

### 3-2. 회사 내규는 김아이가 매번 지켜야 할 공통 기준입니다.

Screen anchors:

```text
출퇴근 시간
업무 보고 체계
징계 기준
```

Presenter flow:

```text
회사 내규는 이번 보고서 자료가 아니다.
출퇴근 시간, 업무 보고 체계, 징계 기준처럼 어떤 업무를 하든 기본으로 지켜야 하는 기준이다.
이미 본 회사 내규(CLAUDE.md)가 왜 자료나 지시와 다른 자리인지 설명한다.
```

Bridge:

```text
반대로 김아이 책상에는 지금 할 일에 필요한 것만 올립니다.
```

### 3-3. 김아이 책상에는 지금 할 일에 필요한 것만 올립니다.

Screen anchors:

```text
작업 지시
작업에 필요한 자료
작업 체크리스트
```

Presenter flow:

```text
이 슬라이드는 Context를 회사 책상 비유로 다시 연결한다.
작업 지시는 지금 무엇을 하라는 말이고, 작업에 필요한 자료와 체크리스트는 이번 일을 완성하기 위한 자료다.
책상 위 자료는 이번 작업을 돕기 위한 것이지 회사 내규가 아니다.
```

Bridge:

```text
작업 중인 책상에 여러 일이 섞이면 판단이 흔들립니다.
```

### 3-4. 작업 중인 책상에 여러 일이 섞이면 판단이 흔들립니다.

Screen anchors:

```text
중간에 바뀐 내용
이전 작업 자료
동시에 열린 두 업무
```

Presenter flow:

```text
Context가 많다는 것보다 섞이는 것이 문제다.
중간에 바뀐 내용이 정리되지 않거나 이전 작업 자료가 남으면 현재 작업의 기준이 흐려진다.
같은 업무환경에서 두 가지 일을 동시에 열어 두면 김아이는 어느 쪽을 기준으로 해야 할지 헷갈린다.
```

Bridge:

```text
하지만 반복해서 확인된 협업 기준은 회사 내규로 올릴 수 있습니다.
```

### 3-5. 반복해서 확인된 협업 기준은 내규로 올릴 수 있습니다.

Screen anchors:

```text
요청 양식
보고 라인
책임 소재
```

Presenter flow:

```text
처음 하는 타부서 협업은 시행착오가 있다.
반복하다 보면 어떤 양식으로 요청해야 하는지, 누구에게 보고해야 하는지, 책임 소재가 어디인지가 정리된다.
이런 기준은 실행 순서 전체가 아니라 다음에도 지킬 협업 기준이므로 내규로 올릴 수 있다.
```

Bridge:

```text
CLAUDE.md에도 적용 범위가 있습니다.
```

### 3-6. CLAUDE.md에도 적용 범위가 있습니다.

Screen anchors:

```text
global = 연방법
user = 주법
project = 회사 사규
subfolder = 부서 규칙
```

Presenter flow:

```text
global CLAUDE.md는 연방법처럼 가장 넓게 깔리는 기본 규칙이다.
user CLAUDE.md는 주법처럼 특정 사용자에게 항상 적용되는 기본 업무 방식이다.
project CLAUDE.md는 회사 사규, subfolder CLAUDE.md는 부서 규칙처럼 범위가 좁아질수록 구체화된다.
```

Bridge:

```text
CLAUDE.md의 적용 강도도 적용 범위에 따라 달라집니다.
```

### 3-7. CLAUDE.md의 적용 강도도 적용 범위에 따라 달라집니다.

Screen anchors:

```text
로드 순서: global → user → project → subfolder
적용 강도: subfolder → project → user → global
가까운 규칙 우선
```

Presenter flow:

```text
로드는 넓은 범위에서 좁은 범위로 진행된다: global, user, project, subfolder 순서다.
하지만 적용 강도는 반대로 이해하면 쉽다. 지금 작업 위치에 가까운 subfolder 규칙이 가장 구체적이고 강하다.
그래서 충돌이 있을 때는 현재 작업 위치에 더 가까운 CLAUDE.md를 먼저 확인한다.
```

Bridge:

```text
내규가 너무 많으면 김아이는 일을 시작하기 어려워집니다.
```

### 3-8. 내규가 너무 많으면 김아이는 일을 시작하기 어려워집니다.

Screen anchors:

```text
양이 많으면 안 읽음
이전 설정과 충돌
실행이 느려짐
```

Presenter flow:

```text
내규는 많을수록 좋은 것이 아니다.
양이 많으면 사람도 안 읽고 AI도 중요한 기준을 놓치기 쉽다.
이전 설정과 현재 설정이 충돌하거나 제약이 너무 많으면 김아이는 일을 시작하기 어려워진다.
```

Bridge:

```text
이제 실습에서는 김아이의 CLAUDE.md를 직접 설정합니다.
```

### 3-9. 이제 실습에서는 김아이의 CLAUDE.md를 직접 설정합니다.

Screen anchors:

```text
적용 범위 선택
내규 초안 작성
과한 내규 줄이기
```

Presenter flow:

```text
이 슬라이드는 실습 UI가 아니라 전환 슬라이드다.
다음 화면에서는 업무 지시와 자료를 분류하는 것이 아니라 CLAUDE.md를 직접 설정한다.
적용 범위를 고르고, 반복해서 지킬 내규를 짧게 쓰고, 과한 내규를 줄인다.
```

Bridge:

```text
마지막으로 내규와 업무 매뉴얼도 구분해야 합니다.
```

### 3-10. 내규는 지켜야 할 기준이고, 매뉴얼은 따라 할 순서입니다.

Screen anchors:

```text
내규 = 기준
매뉴얼 = 순서
다음 Act = 반복 실행
```

Presenter flow:

```text
Act 3를 닫고 Act 4 문제를 연다.
내규는 지켜야 할 기준이고, 매뉴얼은 따라 할 실행 순서다.
다음 Act에서는 반복 실행 순서를 업무 매뉴얼로 따로 만든다.
```

Bridge:

```text
다음 Act에서는 반복 실행 순서를 업무 매뉴얼로 따로 만듭니다.
```

## 3. 실습 계약 연결

| 강의 구간 | 실습 계약에서 확인할 것 |
| --- | --- |
| 3-6 | 수강생이 CLAUDE.md 적용 범위를 이해한다. |
| 3-7 | 로드 순서와 적용 강도를 구분한다. |
| 3-8 | 과한 내규와 충돌 내규를 줄인다. |
| 3-9 | 별도 실습 화면에서 CLAUDE.md 적용 범위를 고르고 내규 초안을 작성한다. |

## 4. 검증 체크리스트

- 9번 슬라이드가 분류 실습이 아니라 CLAUDE.md 설정 실습으로 연결되는가?
- 적용 범위 선택, 내규 초안 작성, 과한 내규 줄이기가 화면 앵커에 보이는가?
- 실습 UI, 점수판, 입력 폼, 검증 로그가 설명 슬라이드에 들어가지 않는가?
